---
export interface Props {
    class?: string
}

const classStr = Astro.props.class || ''
---
<code class={'bg-gray-100 dark:bg-black p-1 rounded-xl' + (classStr.length > 0 ? ' ' : '') + classStr}><slot/></code>